import React from 'react';

const BarChartDemo = ({ productReviews }) => {
  console.log(productReviews);
  // const sample = productReviews.map((review) => review.rating);
  // console.log(sample);
  return (
    // <g></g>
    // <ul>
    //   {sample.map((rating) => <p>{rating}</p>)}
    // </ul>
    <p>ratings array</p>
  );
};

export default BarChartDemo;