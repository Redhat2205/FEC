function sum(a, b) {
  return a + b;
}

const doubled = (val) => {return val * 2};

const halved = (val) => {return val * 0.5};




module.exports.sum = sum;
module.exports.doubled = doubled;
module.exports.halved = halved;